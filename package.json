{
  "name": "thoughts",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "prepare": "husky"
  },
  "devDependencies": {
    "@thoughts/eslint-config": "workspace:*",
    "@thoughts/typescript-config": "workspace:*",
    "husky": "^9.1.5",
    "turbo": "^2.1.0"
  },
  "packageManager": "pnpm@9.1.4",
  "engines": {
    "node": ">=20"
  }
}
